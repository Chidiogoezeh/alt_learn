# Create z vsriable where the user will store the number they want to compute. The user can run\
# diffferent mathematical operations +, -, *, /

num1 = input("Enter a number: ") # Know that python automatically converts user input to a string
num2 = input("Enter another number: ")
result = float(num1) + floa(num2) # Use int or float function to convert num to integers or floats

print(result)